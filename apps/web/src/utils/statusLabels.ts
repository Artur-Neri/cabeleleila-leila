/** Rótulos em pt-BR para valores de enum da API (estado do agendamento). */
export const appointmentStatusLabel: Record<string, string> = {
  pending_confirmation: "Aguardando confirmação",
  confirmed: "Confirmada",
  cancelled: "Cancelada",
};

/** Rótulos em pt-BR para estado operacional de uma linha de serviço. */
export const operationalStatusLabel: Record<string, string> = {
  pending: "Pendente",
  in_progress: "Em andamento",
  completed: "Concluído",
};

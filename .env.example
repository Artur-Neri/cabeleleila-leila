# API local (fora do Docker)
DATABASE_URL=postgresql://cabeleleila:cabeleleila@localhost:5432/cabeleleila?schema=public
JWT_SECRET=altere-isto-em-producao
PORT=3001
HOST=0.0.0.0

# Web (build produção; em dev o Vite usa proxy)
# VITE_API_BASE=/api

# Docker Compose (opcional — compose já define defaults para postgres)
# JWT_SECRET=...

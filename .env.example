# API em desenvolvimento local (sem Docker na aplicação)
DATABASE_URL=postgresql://cabeleleila:cabeleleila@localhost:5432/cabeleleila?schema=public
# Em produção, use um segredo longo, aleatório e privado (não commite).
JWT_SECRET=altere-isto-em-producao
PORT=3001
HOST=0.0.0.0

# Front-end (build de produção; em desenvolvimento o Vite usa proxy)
# VITE_API_BASE=/api

# Docker Compose (opcional — o Compose já define valores padrão para o Postgres)
# JWT_SECRET=...
